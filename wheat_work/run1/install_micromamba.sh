# NOTE

# you only need to do this once.

# when you are logged in, make a folder called softarew in your working directory

# go there

# they paste this command
"${SHELL}" <(curl -L micro.mamba.pm/install.sh)

# for each question, enter "y" 

# the type this to reload shell
source ~/.bashrc

# then test if micromamba is installed for you
micromamba --version

# or look at all commands
micromamba

# if this works, you never need to come back to this page.